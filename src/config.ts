const PORT = process.env.SERVER_PORT;
const MONGODB_URI = process.env.MONGODB_URI;
const DB_NAME = process.env.DB_NAME;
const CORS_ORIGIN = process.env.CORS_ORIGIN;

export { PORT, MONGODB_URI, DB_NAME, CORS_ORIGIN };
